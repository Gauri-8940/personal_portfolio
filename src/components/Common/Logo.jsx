import { Typography } from "@mui/material";

const Logo = () => {
  return (
    <Typography variant="h5" fontWeight={700} color="primary">
      Gauri.
    </Typography>
  );
};

export default Logo;
